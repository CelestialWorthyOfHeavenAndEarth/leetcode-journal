<div align="center">

[![Typing SVG](https://readme-typing-svg.herokuapp.com?font=JetBrains+Mono&weight=500&size=24&pause=1000&color=FF6B2B&center=true&vCenter=true&width=600&lines=%E2%AC%A1+15.%203Sum;Difficulty:+Medium;Topic:+Arrays)](https://git.io/typing-svg)

[![Difficulty](https://img.shields.io/badge/Medium-orange?style=for-the-badge&logoColor=white)](https://leetcode.com/problems/3sum/)
[![Topic](https://img.shields.io/badge/Topic-Arrays-5865F2?style=for-the-badge)](.)
[![Language](https://img.shields.io/badge/Language-C%2B%2B-00599C?style=for-the-badge&logo=cplusplus&logoColor=white)](.)
[![Solved](https://img.shields.io/badge/Solved-2026-09-22-4caf84?style=for-the-badge&logo=github)](.)

</div>

<br>

> [!NOTE]
> **Problem Statement**
>
> Given an integer array nums, return all the triplets [nums[i], nums[j], nums[k]] such that i != j, i != k, and j != k, and nums[i] + nums[j] + nums[k] == 0.
> 
> Notice that the solution set must not contain duplicate triplets.
> 
>  
> Example 1:
> 
> Input: nums = [-1,0,1,2,-1,-4]
> Output: [[-1,-1,2],[-1,0,1]]
> Explanation: 
> nums[0] + nums[1] + nums[2] = (-1) + 0 + 1 = 0.
> nums[1] + nums[2] + nums[4] = 0 + 1 + (-1) = 0.
> nums[0] + nums[3] + nums[4] = (-1) + 2 + (-1) = 0.
> The distinct triplets are [-1,0,1] and [-1,-1,2].
> Notice that the order of the output and the order of the triplets does not matter.
> 
> 
> Examp...
>
> **[→ Open on LeetCode](https://leetcode.com/problems/3sum/)**

<br>

### Solution (C++)

```cpp
class Solution {
public:
    vector<vector<int>> threeSum(vector<int>& nums) {
        int n = nums.size();
        sort(nums.begin(),nums.end());
        vector<vector<int>>ans;
        for(int i=0;i<n;i++){
            if(i!=0&&nums[i]==nums[i-1])continue;
            int Target = 0-nums[i];
            int left=i+1;
            int right=n-1;

            while(left<right){
                if(nums[left]+nums[right]==Target){
                    ans.push_back({nums[i],nums[left],nums[right]});
                    int temp1=nums[left];
                    int temp2=nums[right];
                    while(temp1==nums[left]&&left<right){
                        left++;
                    }
                    while(temp2==nums[right]&&left<right){
                        right--;
                    }
                   
                }
                else if(nums[left]+nums[right]<Target){
                   left++;
                }
                else {
                    right--;
                }
            }

        } 
        return ans;
    }
};
```

<br>

### Approach

> The solution to the 3Sum problem can be solved using a combination of two nested loops and a few if-else statements. The main idea is to iterate through the array and check if the sum of any two elements is equal to the target value. If the sum is less than the target, increment the left index. If the sum is greater than the target, decrement the right index. If the sum is equal to the target, add the triplet to the result vector and reset the left and right indices.

<details>
<summary><strong>Why It Works (Click to expand)</strong></summary>
<br>

> The solution works by iterating through the array and checking if the sum of any two elements is equal to the target value. If the sum is less than the target, increment the left index. If the sum is greater than the target, decrement the right index. If the sum is equal to the target, add the triplet to the result vector and reset the left and right indices.

</details>

<br>

### Complexity

| Bound | Explanation |
|:--|:--|
| **Time:** `The time complexity of this solution is O(n^2), where n is the size of the input array. This is because we iterate through the array twice, once for each element.` |  |
| **Space:** `The space complexity of this solution is O(n), where n is the size of the input array. This is because we only use a vector to store the result and do not require any additional data structures.` |  |

<br>

> [!TIP]
> **Key Insight**
> 
> The key insight behind this solution is to use two nested loops to iterate through the array and check the sum of each pair of elements. If the sum is equal to the target, add the triplet to the result vector and reset the left and right indices.

<br>

> [!IMPORTANT]
> **Pattern to Remember**
>
> The pattern to remember from this solution is to use a nested loop structure with if-else statements to handle different cases of the sum.

<br>

<div align="center">

*Generated by [⬡ LeetCode Journal](https://github.com/CelestialWorthyOfHeavenAndEarth/leetcode-journal)*

</div>
